import { NombreBloqueoMotorPipe } from './nombre-bloqueo-motor.pipe';

describe('NombreBloqueoMotorPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreBloqueoMotorPipe();
    expect(pipe).toBeTruthy();
  });
});
