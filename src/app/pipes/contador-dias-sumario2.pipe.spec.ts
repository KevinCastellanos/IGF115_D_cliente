import { ContadorDiasSumario2Pipe } from './contador-dias-sumario2.pipe';

describe('ContadorDiasSumario2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ContadorDiasSumario2Pipe();
    expect(pipe).toBeTruthy();
  });
});
