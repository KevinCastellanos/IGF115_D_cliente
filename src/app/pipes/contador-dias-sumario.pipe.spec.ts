import { ContadorDiasSumarioPipe } from './contador-dias-sumario.pipe';

describe('ContadorDiasSumarioPipe', () => {
  it('create an instance', () => {
    const pipe = new ContadorDiasSumarioPipe();
    expect(pipe).toBeTruthy();
  });
});
