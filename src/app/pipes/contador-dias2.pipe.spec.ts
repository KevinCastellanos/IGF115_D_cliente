import { ContadorDias2Pipe } from './contador-dias2.pipe';

describe('ContadorDias2Pipe', () => {
  it('create an instance', () => {
    const pipe = new ContadorDias2Pipe();
    expect(pipe).toBeTruthy();
  });
});
