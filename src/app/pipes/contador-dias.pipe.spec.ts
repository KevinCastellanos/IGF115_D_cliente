import { ContadorDiasPipe } from './contador-dias.pipe';

describe('ContadorDiasPipe', () => {
  it('create an instance', () => {
    const pipe = new ContadorDiasPipe();
    expect(pipe).toBeTruthy();
  });
});
