import { FilterDriverPipe } from './filter-driver.pipe';

describe('FilterDriverPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDriverPipe();
    expect(pipe).toBeTruthy();
  });
});
