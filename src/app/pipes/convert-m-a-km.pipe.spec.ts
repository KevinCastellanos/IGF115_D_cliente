import { ConvertMAKmPipe } from './convert-m-a-km.pipe';

describe('ConvertMAKmPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertMAKmPipe();
    expect(pipe).toBeTruthy();
  });
});
