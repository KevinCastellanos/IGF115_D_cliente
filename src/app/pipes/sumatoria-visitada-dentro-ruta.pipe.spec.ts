import { SumatoriaVisitadaDentroRutaPipe } from './sumatoria-visitada-dentro-ruta.pipe';

describe('SumatoriaVisitadaDentroRutaPipe', () => {
  it('create an instance', () => {
    const pipe = new SumatoriaVisitadaDentroRutaPipe();
    expect(pipe).toBeTruthy();
  });
});
