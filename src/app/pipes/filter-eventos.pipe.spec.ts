import { FilterEventosPipe } from './filter-eventos.pipe';

describe('FilterEventosPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEventosPipe();
    expect(pipe).toBeTruthy();
  });
});
