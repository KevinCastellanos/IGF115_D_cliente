import { FilterDragPipe } from './filter-drag.pipe';

describe('FilterDragPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDragPipe();
    expect(pipe).toBeTruthy();
  });
});
