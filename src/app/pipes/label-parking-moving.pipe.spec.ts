import { LabelParkingMovingPipe } from './label-parking-moving.pipe';

describe('LabelParkingMovingPipe', () => {
  it('create an instance', () => {
    const pipe = new LabelParkingMovingPipe();
    expect(pipe).toBeTruthy();
  });
});
