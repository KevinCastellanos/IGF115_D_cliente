import { NombreBloqueoSalida2Pipe } from './nombre-bloqueo-salida2.pipe';

describe('NombreBloqueoSalida2Pipe', () => {
  it('create an instance', () => {
    const pipe = new NombreBloqueoSalida2Pipe();
    expect(pipe).toBeTruthy();
  });
});
